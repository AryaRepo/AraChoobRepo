<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/headerOrderDetailActivity"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        android:background="@color/colorPrimary"
        android:gravity="center|right"
        android:paddingRight="15dp"
        android:text="اطلاعات سفارش"
        android:textColor="#FFF"
        android:textSize="15sp"/>



    <ListView
        android:id="@+id/listOderDetail"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@android:color/transparent"
        android:dividerHeight="10.0sp"
        android:layout_below="@id/headerOrderDetailActivity"
        android:paddingBottom="12dp"
        android:paddingTop="12dp"
        android:paddingRight="20dp"
        android:layout_above="@id/bottomInvoice"
        android:paddingLeft="20dp"
        />

    <RelativeLayout
        android:id="@+id/bottomInvoice"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:background="@color/colorPrimaryDark">

        <TextView
            android:id="@+id/txtOrderDetailTotalPrice"
            android:layout_width="match_parent"
            android:layout_height="match_parent"

            android:gravity="center_horizontal|center_vertical"
            android:textColor="#fff"

            android:textSize="15sp"/>
    </RelativeLayout>

</RelativeLayout>
